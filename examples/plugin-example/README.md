# Custom Converter Plugin Example

Demonstrates how to implement a custom `Converter` for a fictional format and register it with `MarkItDown`.

## Run

```bash
cd examples/plugin-example
npm install
npx tsx index.ts
```
