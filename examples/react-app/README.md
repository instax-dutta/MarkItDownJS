# React App Example

Demonstrates using MarkItDownJS React components (`DocumentDropzone`, `MarkdownViewer`, `ConversionProgress`) and hooks (`useDocumentParser`).

## Run

```bash
cd examples/react-app
npm install
npm run dev
```
