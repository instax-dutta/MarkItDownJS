# RAG Chunking Example

Demonstrates MarkItDownJS's built-in document chunking for RAG pipelines. Convert a document, split it into semantic chunks, and prepare them for embedding.

## Run

```bash
cd examples/rag-chunking
npm install
npx tsx index.ts
```
