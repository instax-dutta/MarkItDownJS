# Browser Usage Example

Demonstrates running MarkItDownJS directly in the browser using Vite. Drag and drop a file to convert it to Markdown — no server needed!

## Run

```bash
cd examples/browser-usage
npm install
npm run dev
```
