---
"@markitdownjs/all": minor
"markitdownjs": minor
---

Add `@markitdownjs/all` umbrella package that depends on and re-exports every `@markitdownjs/*` package, including a `createMarkItDown()` preset factory.

Add `markitdownjs` unscoped entry point that re-exports everything from `@markitdownjs/all`, providing a single `npm install markitdownjs` dependency for all MarkItDownJS features.
