---
"@markitdownjs/pdf": patch
---

Update `pdfjs-dist` from `^4.0.0` to `^5.0.0`. This is a major dependency bump with no breaking API changes in the PDF converter — all 6 converter tests pass on v5.7.284.
